#include<stdio.h>
#include<math.h>

//min and max step values
struct Limits{
    short unsigned minimum;
    short unsigned maximum;
};

//method declaration
int calculateTotalRewards(struct Limits);
